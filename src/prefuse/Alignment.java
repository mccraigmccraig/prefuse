/**
 *
 */
package prefuse;

/**
 * An enum representing horizontal/vertical alignment.
 *
 * @author Anton Marsden
 *
 */
public enum Alignment {
	/* inside bounds */
	LEFT, RIGHT, CENTER, BOTTOM, TOP,
	/* outside of bounds */
	FAR_LEFT, FAR_RIGHT, FAR_BOTTOM, FAR_TOP
}